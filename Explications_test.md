# Test technique d'intégration avec Trilogy

# Etapes réalisées
1. Clonage du repo fourni par le recruteur.
2. Installation des dépendances avec la commmande 'npm i'
3. Lancement du serveur local avec la commande 'npm run dev'.
4. Création d'une nouvelle page 'TestPage.jsx' dans le dossier 'src'.
5. Import de 'TestPage' dans le fichier 'page.tsx' (qui se trouve dans le dossier app sous le dossier 'src').
6. Ajout des composants Trilogy (Section, Hero, Columns, etc.) dans le fichier 'TestPage.jsx'.
7. Préparation du code pour push sur mon fork GitHub.
8. Rédaction de ce fichier pour expliquer mon travail.

# Remarques
- Header et Footer non intégrés (selon consignes).
- L'image '/screen-mobile.png' se trouve dans le dossier public.

# Problème rencontré
- La page ne s'affiche pas localement avec la commande 'npm run dev' (Le serveur Next.js ('localhost:3000') démarre bien, mais les composants Trilogy ne s'affiche pas sans cette dépendance).

# Conclusion
Le code source est pret et organisé, mais l'affichage local est bloqué. Tout le reste du projet est fonctionnel et pret pour évaluation.